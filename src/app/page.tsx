import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
    return (
        <div>
            page
            <Button>Helloo</Button>
        </div>
    );
};

export default page;
